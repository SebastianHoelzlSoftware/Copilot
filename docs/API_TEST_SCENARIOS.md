# Scenarios for API Test

## Registration

1. A new user registers via the public /api/register endpoint with invalid data -> Error
2. He registers with valid data -> Received UserId, CustomerId and ContactId
3. The user creates a project brief
4. A developer user updates that project brief
5. The user updates the project brief
